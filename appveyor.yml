version: '{build}'
branches:
  # whitelist
  only:
    - master
    - dev
image: 
  - Visual Studio 2019
  - Ubuntu

build_script:
- ps: |
    if ($isLinux) {
      ./build.sh
    } else {
      ./build.ps1
    }